{
  "version": 2,
  "outputDirectory": "dist/public",
  "buildCommand": "pnpm vite build && pnpm esbuild server/index.ts --platform=node --packages=external --bundle --format=esm --outdir=dist --tsconfig=server/tsconfig.json",
  "rewrites": [
    { "source": "/api/(.*)", "destination": "/api/$1" },
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}